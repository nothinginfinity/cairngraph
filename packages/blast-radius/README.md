# @cairngraph/blast-radius

Impact analysis engine for CairnStone graph objects.

Phase 2D implements:

- `computeBlastRadius(graph, options)`
- `blastRadiusSubgraph(graph, result)`

Supported roots:

- node id
- stone hash
- ref id
- default HEAD node

Supported traversal directions:

- outgoing
- incoming
- both
