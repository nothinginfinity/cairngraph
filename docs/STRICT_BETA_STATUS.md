# Strict Beta Status

Strict beta live-provider verification is green.

Verified:

```text
Live Provider Config with require_live_provider=true
Live Chain Verification with require_live_provider=true
```

Configured Worker variable:

```text
CAIRNSTONE_V5_BASE_URL=https://cairnstone-v5.jaredtechfit.workers.dev
```

Meaning:

```text
/health reports cairnstone-v5 as configured
/graph/chain/cairngraph returns live graph JSON
/graph/chain/cairngraph/html returns interactive live graph HTML
```

Current strict beta status:

```text
live CairnStone provider verified
live chain endpoints verified
interactive HTML shell verified against live chain data
```

Not yet claimed:

```text
CairnStone HEAD updated
v1.0 final released
```
