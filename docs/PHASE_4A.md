# Phase 4A: v1.1-alpha.1 Graph Explorer UX

Phase 4A begins the CairnGraph user experience layer.

Verified starting HEAD:

```text
b306bce354b5cab887e7bc79f44064be1dd69a906647c6ff25eb9d0c9606ad1e
```

Starting HEAD title:

```text
cairngraph v1 deployed orientation
```

Added UX capabilities:

```text
embedded SVG graph explorer
node selection sync between list and graph
edge type filter
selected-neighborhood mode
graph node and edge counts
topology view inside standalone HTML renderer
```

Preserved v1.0 contract:

```text
/render/html remains standalone HTML
payload provider remains stable
cairnstone-v5 live provider remains stable
no external frontend framework
no canvas dependency
```

Next UX checkpoints:

```text
v1.1-alpha.2: zoom and pan controls
v1.1-alpha.3: blast-radius visual highlighting
v1.1-alpha.4: source/ref deep-link inspector
```
